<form method="post" action="{{ path('app_blagues_delete', {'id': blague.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ blague.id) }}">
    <button class="btn">Delete</button>
</form>
